- error = @post.errors.include?(:title)
%fieldset.control-group{:class => error ? 'has-error' : ''}
  =f.label :title, :class => 'control-label'
  .controls
    =f.text_field :title, :class => 'form-control input-large input-with-feedback', :autofocus => true
    %span.help-inline=error ? f.error_message_on(:title, :class => 'text-error') : pat(:example)
- error = @post.errors.include?(:sub_title)
%fieldset.control-group{:class => error ? 'has-error' : ''}
  =f.label :sub_title, :class => 'control-label'
  .controls
    =f.text_field :sub_title, :class => 'form-control input-large input-with-feedback'
    %span.help-inline=error ? f.error_message_on(:sub_title, :class => 'text-error') : pat(:example)
- error = @post.errors.include?(:body)
%fieldset.control-group{:class => error ? 'has-error' : ''}
  =f.label :body, :class => 'control-label'
  .controls
    ~f.text_area :body, :class => 'form-control input-large input-with-feedback'
    %span.help-inline=error ? f.error_message_on(:body, :class => 'text-error') : pat(:example)
- error = @post.errors.include?(:imgage)
%fieldset.control-group{:class => error ? 'has-error' : ''}
  =f.label :imgage, :class => 'control-label'
  .controls
    ~f.text_area :imgage, :class => 'form-control input-large input-with-feedback'
    %span.help-inline=error ? f.error_message_on(:imgage, :class => 'text-error') : pat(:example)

.form-actions
  =f.submit pat(:save), :class => 'btn btn-primary'
  &nbsp;
  =f.submit pat(:save_and_continue), :class => 'btn btn-info', :name => 'save_and_continue'
  &nbsp;
  =link_to pat(:cancel), url(:posts, :index), :class => 'btn btn-default'